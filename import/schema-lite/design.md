---
entryPoint: true
---

# Design Data Schema — Entry Point

Load `SCHEMA.md` next. It contains the non-negotiables, layer map,
file roles, task routing, worked examples, and parked work — everything
needed to orient and act. This file exists only to preserve the
`design.md` entry-point convention that skills and agents expect.
